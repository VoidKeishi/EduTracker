import { createTheme } from '@mui/material/styles'

export const themeOption = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#006874'
    },
    secondary: {
      main: '#4A6267'
    }
  }
})
